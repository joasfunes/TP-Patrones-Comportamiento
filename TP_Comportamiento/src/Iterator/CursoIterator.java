package Iterator;

public interface CursoIterator {
    boolean hasNext();  // Verifica si hay más elementos en la colección
    Curso next();       // Devuelve el siguiente elemento en la colección
}
